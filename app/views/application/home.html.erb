<div class="center jumbotron sg-home sg-home1">
  <% if !current_user.nil? && current_user.is_admin? %>
    <%= link_to t("home.cta_manage_cohort"), cohorts_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% elsif !current_user.nil? && current_user.is_judge? %>
    <%= link_to t("home.cta_view_submissions"), root_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% else %>
    <%= link_to t("home.cta_submit_application"), new_submission_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% end %>
</div>

<div class="center jumbotron sg-home sg-home2">
  <% if !current_user.nil? && current_user.is_admin? %>
    <%= link_to t("home.cta_manage_cohort"), cohorts_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% elsif !current_user.nil? && current_user.is_judge? %>
    <%= link_to t("home.cta_view_submissions"), root_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% else %>
    <%= link_to t("home.cta_submit_application"), new_submission_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% end %>
</div>

<div class="center jumbotron sg-home sg-home3">
  <% if !current_user.nil? && current_user.is_admin? %>
    <%= link_to t("home.cta_manage_cohort"), cohorts_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% elsif !current_user.nil? && current_user.is_judge? %>
    <%= link_to t("home.cta_view_submissions"), root_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% else %>
    <%= link_to t("home.cta_submit_application"), new_submission_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% end %>
</div>

<div class="center jumbotron sg-home sg-home4">
  <% if !current_user.nil? && current_user.is_admin? %>
    <%= link_to t("home.cta_manage_cohort"), cohorts_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% elsif !current_user.nil? && current_user.is_judge? %>
    <%= link_to t("home.cta_view_submissions"), root_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% else %>
    <%= link_to t("home.cta_submit_application"), new_submission_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% end %>
</div>

<div class="center jumbotron sg-home sg-home5">
  <% if !current_user.nil? && current_user.is_admin? %>
    <%= link_to t("home.cta_manage_cohort"), cohorts_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% elsif !current_user.nil? && current_user.is_judge? %>
    <%= link_to t("home.cta_view_submissions"), root_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% else %>
    <%= link_to t("home.cta_submit_application"), new_submission_path, class: "cta-submit btn btn-lg btn-primary"%>
  <% end %>
</div>
